stages:
  - test
  - build
  - deploy

code_quality:
  image: node:14.16.1
  stage: test
  before_script:
    - npm ci
  script:
    - npm run eslint
    - npm run prettier-check
  only:
    - merge_requests

build:
  image: node:14.16.1
  stage: build
  before_script:
    - npm ci
  script:
    - npm run build
  artifacts:
    paths:
      - dist
  only:
    - merge_requests
    - main

pages:
  image: node:14.16.1
  stage: deploy
  before_script:
    - npm install
  script:
    - npm run build
    - cp -r dist public
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
